const BANCO = {
 "1":[
  {q:"Define con tus palabras los conceptos de relación, tupla, atributo, grado y cardinalidad. Da un ejemplo de cada uno usando la tabla EMPLOYEES.",p:10,
   k:"Relación = tabla (EMPLOYEES). Tupla = fila (un empleado). Atributo = columna (salary). Dominio = valores válidos de un atributo. Grado = número de columnas (EMPLOYEES tiene 11). Cardinalidad = número de filas."},
  {q:"Explica la diferencia entre superclave, clave candidata, clave primaria y clave alternativa. Ejemplifica con una tabla que tenga más de una clave candidata.",p:10,
   k:"Superclave: identifica de forma única, puede tener atributos de sobra. Clave candidata: superclave mínima. PK: la candidata elegida. Alternativa: las candidatas no elegidas. Ej: en EMPLOYEES, employee_id y email son ambas candidatas; se elige employee_id como PK y email queda como alternativa (UNIQUE)."},
  {q:"Enuncia las dos reglas de integridad del modelo relacional y explica qué pasaría si cada una se violara.",p:10,
   k:"Integridad de entidad: la PK no puede ser NULL ni duplicada; si se violara no podrías identificar filas de forma única. Integridad referencial: toda FK debe apuntar a una PK existente o ser NULL; si se violara tendrías registros huérfanos apuntando a algo inexistente."},
  {q:"Dibuja el modelo relacional para una BIBLIOTECA: libros, autores, socios y préstamos. Indica PK, FK y cardinalidad de cada relación. Justifica dónde aparece una relación N:M y cómo la resuelves.",p:20,
   k:"LIBROS(libro_id PK, titulo, isbn). AUTORES(autor_id PK, nombre). LIBRO_AUTOR(libro_id FK, autor_id FK, PK compuesta) resuelve la N:M porque un libro puede tener varios autores y un autor varios libros. SOCIOS(socio_id PK, nombre). PRESTAMOS(prestamo_id PK, libro_id FK, socio_id FK, fecha_prestamo, fecha_devolucion): 1:N desde LIBROS y desde SOCIOS."},
  {q:"Explica qué es una relación reflexiva. Identifica las dos que existen en el esquema HR y explica por qué sus claves foráneas deben admitir NULL.",p:15,
   k:"Es una FK que apunta a la PK de su propia tabla. En HR: EMPLOYEES.manager_id → EMPLOYEES.employee_id, y DEPARTMENTS.manager_id → EMPLOYEES.employee_id. Deben admitir NULL porque el empleado del tope de la jerarquía no tiene jefe y porque un departamento puede quedar temporalmente sin gerente."},
  {q:"La siguiente tabla no está normalizada: VENTAS(venta_id, fecha, cliente_id, cliente_nombre, cliente_ciudad, producto_id, producto_nombre, cantidad, precio_unitario). Llévala a 3FN explicando en cada paso qué anomalía eliminas.",p:20,
   k:"1FN: separar los productos repetidos por venta en DETALLE_VENTA(venta_id, producto_id, cantidad, precio_unitario) con PK compuesta. 2FN: producto_nombre depende solo de producto_id, no de la PK completa → tabla PRODUCTOS(producto_id PK, nombre, precio). 3FN: cliente_nombre y cliente_ciudad dependen de cliente_id, que no es clave (dependencia transitiva) → tabla CLIENTES(cliente_id PK, nombre, ciudad). Queda VENTAS(venta_id PK, fecha, cliente_id FK)."},
  {q:"Explica la diferencia entre 2FN y 3FN. ¿Puede una tabla con clave primaria simple violar la 2FN? Justifica.",p:10,
   k:"2FN elimina dependencias parciales de una PK compuesta; 3FN elimina dependencias transitivas entre atributos no clave. Una tabla con PK simple no puede violar 2FN porque no existe «parte» de la clave de la cual depender parcialmente: si está en 1FN, está en 2FN."},
  {q:"En notación pata de gallo, explica qué información aporta cada elemento gráfico: línea simple, horquilla, línea sólida y línea punteada. Aplícalo a la relación DEPARTMENTS–EMPLOYEES.",p:10,
   k:"Línea simple: lado «uno». Horquilla: lado «muchos». Línea sólida: participación obligatoria (FK NOT NULL). Punteada: opcional (FK admite NULL). DEPARTMENTS–EMPLOYEES es 1:N: un departamento tiene muchos empleados y la FK department_id vive en EMPLOYEES, admitiendo NULL para empleados sin departamento asignado."},
  {q:"Describe el procedimiento completo para pasar de un modelo conceptual a un modelo relacional. Enumera las reglas para relaciones 1:1, 1:N y N:M.",p:15,
   k:"Cada entidad → tabla; cada atributo → columna; identificador único → PK. 1:N: la FK va en el lado «muchos». N:M: se crea una tabla intermedia con las dos FK, que juntas forman la PK compuesta. 1:1: la FK va en cualquiera de las dos, preferentemente en la de participación obligatoria. Atributos multivaluados → tabla aparte."},
  {q:"Diseña el modelo relacional para un sistema de VENTAS con clientes, productos, pedidos y detalle de pedido. Explica por qué el detalle de pedido necesita clave primaria compuesta.",p:20,
   k:"CLIENTES(cliente_id PK). PRODUCTOS(producto_id PK, nombre, precio). PEDIDOS(pedido_id PK, fecha, cliente_id FK). DETALLE_PEDIDO(pedido_id FK, producto_id FK, cantidad, precio_unitario) con PK compuesta (pedido_id, producto_id): resuelve la N:M entre pedidos y productos, y garantiza que un producto no se repita dos veces dentro del mismo pedido."},
  {q:"Explica las tres anomalías que evita la normalización, dando para cada una un ejemplo concreto sobre el esquema HR.",p:15,
   k:"Inserción: no poder registrar un departamento nuevo hasta que exista un empleado, si estuvieran en la misma tabla. Actualización: si department_name viviera en EMPLOYEES, renombrar un departamento obligaría a actualizar todas las filas de sus empleados. Borrado: al eliminar al último empleado de un departamento se perdería el nombre de ese departamento."},
  {q:"Enumera las 7 tablas del esquema HR, indicando para cada una su clave primaria y sus claves foráneas.",p:15,
   k:"REGIONS(region_id PK). COUNTRIES(country_id PK, region_id FK). LOCATIONS(location_id PK, country_id FK). DEPARTMENTS(department_id PK, manager_id FK, location_id FK). JOBS(job_id PK). EMPLOYEES(employee_id PK, job_id FK, manager_id FK reflexiva, department_id FK). JOB_HISTORY(PK compuesta employee_id+start_date; FK employee_id, job_id, department_id)."},
  {q:"¿Por qué JOB_HISTORY tiene clave primaria compuesta? ¿Qué problema aparecería si su PK fuera solo employee_id?",p:10,
   k:"Su PK es employee_id + start_date porque un empleado puede haber tenido varios cargos a lo largo del tiempo. Si la PK fuera solo employee_id, cada empleado podría tener un único registro histórico y sería imposible guardar más de un cargo anterior."},
  {q:"Explica qué es un dominio en el modelo relacional y cómo se implementa en Oracle. Da tres ejemplos con columnas del esquema HR.",p:10,
   k:"Un dominio es el conjunto de valores válidos que puede tomar un atributo. En Oracle se implementa con el tipo de dato más restricciones (CHECK, NOT NULL, UNIQUE). Ejemplos: salary NUMBER con CHECK (salary > 0); email VARCHAR2(25) UNIQUE; hire_date DATE que no admite texto."}
 ],
 "2":[
  {q:"Escribe una consulta que muestre nombre, apellido, nombre del departamento y años de antigüedad de los empleados con sueldo superior a 20.000, ordenados por departamento.",p:15,
   k:"SELECT first_name, last_name, department_name, ROUND(MONTHS_BETWEEN(SYSDATE, hire_date)/12) FROM employees JOIN departments USING(department_id) WHERE salary > 20000 ORDER BY department_name;"},
  {q:"Explica el orden lógico de ejecución de un SELECT y justifica por qué no puedes usar un alias definido en el SELECT dentro de la cláusula WHERE.",p:10,
   k:"Orden: FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY. El WHERE se evalúa antes que el SELECT, por lo que el alias todavía no existe en ese momento. En cambio ORDER BY sí puede usarlo porque se ejecuta después del SELECT."},
  {q:"¿Cuál es la diferencia entre JOIN ... ON y JOIN ... USING? Escribe la misma consulta de las dos formas y señala una restricción de USING.",p:10,
   k:"ON permite cualquier condición y nombres de columna distintos. USING exige que la columna se llame igual en ambas tablas, la muestra una sola vez y prohíbe anteponerle alias. Ej: JOIN departments USING(department_id) equivale a JOIN departments d ON e.department_id = d.department_id."},
  {q:"Escribe una consulta que cuente cuántos empleados hay por departamento mostrando el nombre del departamento, incluyendo también los departamentos sin ningún empleado.",p:15,
   k:"SELECT d.department_name, COUNT(e.employee_id) FROM departments d LEFT JOIN employees e ON d.department_id = e.department_id GROUP BY d.department_name; Debe ser LEFT JOIN y contar una columna de employees, no COUNT(*), para que los departamentos vacíos den 0."},
  {q:"Escribe una consulta que muestre los departamentos cuyo sueldo promedio supere los 8.000. Explica por qué esa condición no puede ir en el WHERE.",p:15,
   k:"SELECT department_id, AVG(salary) FROM employees GROUP BY department_id HAVING AVG(salary) > 8000; No puede ir en WHERE porque este se evalúa antes del GROUP BY, cuando los grupos y por tanto el promedio aún no existen."},
  {q:"Explica la diferencia entre INNER JOIN, LEFT JOIN, RIGHT JOIN y FULL OUTER JOIN, dando un ejemplo del esquema HR donde el resultado cambie.",p:15,
   k:"INNER: solo coincidencias. LEFT: todas las de la izquierda más las coincidencias. RIGHT: todas las de la derecha. FULL: todas las de ambos lados. Ejemplo: employees LEFT JOIN departments muestra empleados sin departamento; departments LEFT JOIN employees muestra departamentos sin empleados; con INNER ambos desaparecen."},
  {q:"Escribe la sentencia DDL para crear la tabla CURSOS con: id numérico como PK, nombre de texto obligatorio de máximo 100 caracteres, cupo numérico con valor por defecto 30, y una FK profesor_id que referencie a EMPLOYEES.",p:15,
   k:"CREATE TABLE cursos (curso_id NUMBER PRIMARY KEY, nombre VARCHAR2(100) NOT NULL, cupo NUMBER DEFAULT 30, profesor_id NUMBER, CONSTRAINT fk_curso_prof FOREIGN KEY (profesor_id) REFERENCES employees(employee_id)); En Oracle VARCHAR2, no VARCHAR."},
  {q:"Explica qué es NULL en SQL. ¿Por qué COUNT(*) y COUNT(commission_pct) dan resultados distintos? ¿Cómo se compara un valor con NULL?",p:10,
   k:"NULL es ausencia de valor, no cero ni cadena vacía. COUNT(*) cuenta filas; COUNT(columna) ignora los NULL de esa columna, y muchos empleados no tienen comisión. No se compara con = sino con IS NULL / IS NOT NULL. NVL(col, valor) lo sustituye."},
  {q:"Escribe una consulta que muestre el nombre del empleado, su departamento y la ciudad donde trabaja. Explica por qué se necesita más de un JOIN.",p:15,
   k:"SELECT e.first_name, d.department_name, l.city FROM employees e JOIN departments d ON e.department_id = d.department_id JOIN locations l ON d.location_id = l.location_id; Se necesitan dos JOIN porque no hay FK directa entre EMPLOYEES y LOCATIONS: la ruta pasa obligatoriamente por DEPARTMENTS."},
  {q:"Escribe las sentencias DML para: (a) insertar un empleado nuevo, (b) subir 10% el sueldo de los del departamento 60, (c) eliminar los empleados sin departamento asignado.",p:15,
   k:"(a) INSERT INTO employees (employee_id, first_name, last_name, email, hire_date, job_id, salary) VALUES (500,'Ana','Soto','ASOTO',SYSDATE,'IT_PROG',5000); (b) UPDATE employees SET salary = salary * 1.1 WHERE department_id = 60; (c) DELETE FROM employees WHERE department_id IS NULL; Todo requiere COMMIT para hacerse permanente."},
  {q:"Menciona cinco diferencias de sintaxis entre Oracle y otros motores como MySQL o PostgreSQL, dando el equivalente de cada una.",p:10,
   k:"VARCHAR2 en vez de VARCHAR; SYSDATE en vez de NOW()/CURRENT_DATE; secuencias en vez de AUTO_INCREMENT; ROWNUM o FETCH FIRST n ROWS ONLY en vez de LIMIT; tabla DUAL para consultar expresiones sin tabla; NVL en vez de IFNULL; concatenación con || en vez de CONCAT."},
  {q:"Escribe una consulta que liste los empleados contratados en los últimos 5 años, mostrando su antigüedad en años redondeada.",p:10,
   k:"SELECT first_name, last_name, ROUND(MONTHS_BETWEEN(SYSDATE, hire_date)/12) AS anios FROM employees WHERE MONTHS_BETWEEN(SYSDATE, hire_date)/12 <= 5 ORDER BY hire_date DESC; También sirve WHERE hire_date >= ADD_MONTHS(SYSDATE, -60)."},
  {q:"Explica la diferencia entre WHERE y HAVING y escribe una consulta que use ambas en la misma sentencia.",p:15,
   k:"WHERE filtra filas antes de agrupar; HAVING filtra grupos después. Ej: SELECT department_id, AVG(salary) FROM employees WHERE job_id <> 'AD_PRES' GROUP BY department_id HAVING AVG(salary) > 6000; el WHERE excluye al presidente antes de calcular, el HAVING descarta grupos con promedio bajo."},
  {q:"Escribe una consulta que muestre el nombre completo en una sola columna (nombre y apellido separados por espacio) junto al cargo de cada empleado.",p:10,
   k:"SELECT e.first_name || ' ' || e.last_name AS nombre_completo, j.job_title FROM employees e JOIN jobs j ON e.job_id = j.job_id; En Oracle la concatenación es con ||."}
 ],
 "3":[
  {q:"Escribe una consulta que muestre los empleados que ganan más que el promedio de su propio departamento. Explica por qué la subconsulta es correlacionada.",p:20,
   k:"SELECT e.first_name, e.salary FROM employees e WHERE e.salary > (SELECT AVG(s.salary) FROM employees s WHERE s.department_id = e.department_id); Es correlacionada porque la subconsulta referencia e.department_id de la consulta externa, por lo que se reevalúa para cada fila."},
  {q:"Explica la diferencia entre subconsulta simple, correlacionada y subconsulta en la cláusula FROM. Da un ejemplo de cada una.",p:15,
   k:"Simple: se ejecuta una vez, su resultado se usa en la externa (WHERE salary > (SELECT AVG(salary) FROM employees)). Correlacionada: referencia la consulta externa y se evalúa por fila. En FROM (vista en línea): actúa como tabla temporal, ej. SELECT * FROM (SELECT department_id, AVG(salary) prom FROM employees GROUP BY department_id) WHERE prom > 8000."},
  {q:"Escribe la sentencia para crear una vista que muestre nombre, cargo y departamento de los empleados sin exponer sueldos. Explica dos usos reales de una vista.",p:15,
   k:"CREATE VIEW v_empleados_publico AS SELECT e.first_name, e.last_name, j.job_title, d.department_name FROM employees e JOIN jobs j ON e.job_id=j.job_id JOIN departments d ON e.department_id=d.department_id; Usos: restringir columnas sensibles sin dar acceso a la tabla, y encapsular consultas complejas repetidas para simplificar el trabajo diario."},
  {q:"Explica qué son las propiedades ACID. Para cada una, describe qué fallaría en un sistema bancario si no se cumpliera.",p:20,
   k:"Atomicidad: sin ella el dinero saldría de una cuenta y no entraría en la otra. Consistencia: sin ella el total de dinero del sistema cambiaría solo. Aislamiento: sin él dos transferencias simultáneas leerían saldos desactualizados y se pisarían. Durabilidad: sin ella una caída de energía borraría transferencias ya confirmadas."},
  {q:"Explica el rol de COMMIT, ROLLBACK y SAVEPOINT. ¿Qué pasa en Oracle si haces un DELETE y cierras la sesión sin confirmar?",p:15,
   k:"COMMIT hace permanentes los cambios; ROLLBACK los deshace hasta el último COMMIT o SAVEPOINT; SAVEPOINT marca un punto intermedio para deshacer parcialmente. Si cierras la sesión sin COMMIT, Oracle hace ROLLBACK automático y las filas no se borran."},
  {q:"Escribe un self-join que muestre el nombre de cada empleado junto al de su jefe directo, incluyendo también al empleado que no tiene jefe.",p:15,
   k:"SELECT e.first_name AS empleado, j.first_name AS jefe FROM employees e LEFT JOIN employees j ON e.manager_id = j.employee_id; Debe ser LEFT JOIN para que el presidente aparezca con jefe en NULL."},
  {q:"Explica qué es un índice, cómo mejora el rendimiento y cuál es su costo. ¿Sobre qué columnas del esquema HR crearías índices y por qué?",p:15,
   k:"Es una estructura auxiliar (típicamente árbol B) que evita recorrer toda la tabla. Costo: espacio en disco y penalización en cada INSERT/UPDATE/DELETE. En HR conviene indexar las FK usadas en JOIN (department_id, job_id, manager_id) y columnas muy filtradas como last_name. Las PK ya tienen índice automático."},
  {q:"Explica qué son las restricciones (constraints) en Oracle. Enumera los cinco tipos y escribe un ejemplo de cada uno.",p:15,
   k:"PRIMARY KEY (identifica de forma única), FOREIGN KEY (integridad referencial), UNIQUE (no repite pero admite NULL), NOT NULL (obliga a tener valor), CHECK (condición sobre el valor, ej. CHECK (salary > 0)). Se pueden declarar en columna o a nivel de tabla, y conviene nombrarlas."},
  {q:"Explica qué son ON DELETE CASCADE y ON DELETE SET NULL. Aplica cada uno a una relación del esquema HR y explica la consecuencia.",p:15,
   k:"CASCADE borra las filas hijas al borrar la padre; SET NULL deja la FK en NULL. En HR, al borrar un empleado tendría sentido CASCADE sobre JOB_HISTORY (su historial deja de tener sentido) y SET NULL sobre DEPARTMENTS.manager_id (el departamento sigue existiendo, solo queda sin gerente)."},
  {q:"Escribe una consulta que muestre, para cada departamento, el nombre del departamento, el sueldo máximo y el nombre del empleado que lo recibe.",p:20,
   k:"SELECT d.department_name, e.first_name, e.salary FROM employees e JOIN departments d ON e.department_id = d.department_id WHERE e.salary = (SELECT MAX(s.salary) FROM employees s WHERE s.department_id = e.department_id); Subconsulta correlacionada: no basta con MAX + GROUP BY porque también se pide el nombre."},
  {q:"Compara bases de datos relacionales y no relacionales tomando MongoDB como ejemplo. Explica al menos tres diferencias y en qué caso elegirías cada una.",p:20,
   k:"Relacional: esquema fijo en tablas, integridad referencial, transacciones ACID, consultas con JOIN y SQL estándar, escalado vertical. MongoDB: documentos JSON con esquema flexible, datos duplicados a propósito, sin JOIN nativo, escalado horizontal por sharding, consistencia eventual. Relacional para banca o ERP donde la consistencia es crítica; documental para catálogos variables, logs o volúmenes muy grandes de lectura."},
  {q:"Escribe una consulta que use una subconsulta en la cláusula FROM (vista en línea) para mostrar los departamentos con sueldo promedio superior a 8.000 junto a su nombre.",p:15,
   k:"SELECT d.department_name, x.prom FROM (SELECT department_id, AVG(salary) AS prom FROM employees GROUP BY department_id) x JOIN departments d ON x.department_id = d.department_id WHERE x.prom > 8000;"},
  {q:"Explica qué son las funciones de agregación y cuál es la regla que obliga a usar GROUP BY. ¿Qué error lanza Oracle si no se cumple?",p:10,
   k:"COUNT, SUM, AVG, MIN, MAX operan sobre conjuntos de filas y devuelven un valor por grupo. Toda columna del SELECT que no esté dentro de una función de agregación debe aparecer en el GROUP BY. Si no, Oracle lanza ORA-00979: not a GROUP BY expression."},
  {q:"Escribe las sentencias para modificar una tabla existente: agregar una columna, cambiar su tipo, agregar una restricción y eliminar una columna.",p:15,
   k:"ALTER TABLE cursos ADD (creditos NUMBER); ALTER TABLE cursos MODIFY (nombre VARCHAR2(150)); ALTER TABLE cursos ADD CONSTRAINT chk_cupo CHECK (cupo > 0); ALTER TABLE cursos DROP COLUMN creditos;"}
 ]
};
